# Loaded by the dev server only (vite dev), never by production builds.

# Where each dashboard's dev server runs, for the user menu's dashboard links.
# Not set on the server, where they share one origin.
VITE_DASHBOARD_URLS=/sales-dashboard=http://localhost:5174,/scorecard-dashboard=http://localhost:5175,/distribution=http://localhost:5176
